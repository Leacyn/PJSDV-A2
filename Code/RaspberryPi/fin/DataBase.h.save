#ifndef DATABASE_H
#define DATABASE_H

#include "mysql_connection.h"
#include <string>
#include <cppconn/driver.h>
#include <cppconn/exception.h>
#include <cppconn/resultset.h>
#include <cppconn/statement.h>
#include <cppconn/prepared_statement.h>

class DataBase
{
	public:
		void DataBase(std::string path, std::string user, std::string password, std::string db);
		void addUser(std::string username, std::string password);
		void closeConnection(void);
		void query(void);
	protected:
		sql::Driver *driver;
  		sql::Connection *con;
  		sql::Statement *stmt;
  		sql::ResultSet *res;
  		sql::PreparedStatement *pstmt;
  		void 
sqlError(sql::SQLException e);
};

#endif


